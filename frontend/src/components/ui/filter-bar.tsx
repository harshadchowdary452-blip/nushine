/* ═══════════════════════════════════════════════════════════════════════════
   LEGACY SHIM — `src/components/ui` must not contain new implementations.
   Delegates to the design-system FilterBar compositor.
   ═══════════════════════════════════════════════════════════════════════════ */

import {
  FilterBar,
  FilterChips,
  FilterField,
} from "@/design-system/components/filters"

export { FilterBar, FilterChips, FilterField }
